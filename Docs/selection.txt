Selection is done before filtering it happens 

The selection criteria is done by prefixing the request with a slash or T:.

 tid 
 /title 
 T:title

The /pattern form searches the title and adds all that match

The T:title form search the title for the given type

   (v,r,g,p,a) = Value/Vision, Role, Goal, Project, Action
